//import { askActions } from "./actions";

const initialState = {
  asks: []
};

export function askReducer(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
